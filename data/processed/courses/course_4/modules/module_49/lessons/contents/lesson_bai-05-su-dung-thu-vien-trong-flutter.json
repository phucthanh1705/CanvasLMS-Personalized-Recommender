{
  "id": "lesson_bai-05-su-dung-thu-vien-trong-flutter",
  "title": "lesson_bai-05-su-dung-thu-vien-trong-flutter",
  "body": "<h1>🧩 BÀI 05 – SỬ DỤNG THƯ VIỆN TRONG FLUTTER</h1><h2>🎯 Mục tiêu học tập</h2><ul>  <li>Hiểu khái niệm thư viện (package) trong Flutter và cách sử dụng pub.dev.</li>  <li>Biết cách thêm, cập nhật và quản lý các gói (package) trong dự án Flutter.</li>  <li>Thực hành sử dụng một số thư viện phổ biến: http, provider, shared_preferences, url_launcher, firebase_core.</li>  <li>Hiểu cơ chế dependency, version, null-safety và cấu trúc package trong pubspec.yaml.</li>  <li>Tạo và xuất bản gói (package) Flutter cơ bản lên pub.dev.</li></ul><hr><h2>1. Giới thiệu về thư viện (package)</h2><p>Flutter có hệ sinh thái phong phú với hàng ngàn thư viện trên <a href=\"https://pub.dev\">pub.dev</a>. Thư viện giúp tiết kiệm thời gian lập trình, tăng khả năng tái sử dụng và mở rộng tính năng ứng dụng.</p><p>Các loại thư viện:</p><ul>  <li><b>Package:</b> chứa mã Dart dùng chung.</li>  <li><b>Plugin:</b> có chứa mã native (Android/iOS) để truy cập phần cứng.</li>  <li><b>Flutter plugin:</b> hỗ trợ cả Dart và native code.</li></ul><hr><h2>2. Thêm gói vào dự án</h2><p>Để thêm thư viện, mở file <b>pubspec.yaml</b> và thêm vào phần dependencies:</p><pre><code>dependencies:  flutter:    sdk: flutter  http: ^1.2.0  provider: ^6.1.0  shared_preferences: ^2.2.0</code></pre><p>Sau đó chạy lệnh:</p><pre><code>flutter pub get</code></pre><p>Lệnh này sẽ tải các gói về thư mục <code>.pub-cache</code>.</p><hr><h2>3. Sử dụng gói HTTP để gọi API</h2><pre><code>import 'package:http/http.dart' as http;import 'dart:convert';Future fetchData() async {  var url = Uri.parse('https://jsonplaceholder.typicode.com/posts/1');  var response = await http.get(url);  if (response.statusCode == 200) {    var data = jsonDecode(response.body);    print('Tiêu đề: ${data['title']}');  } else {    print('Lỗi kết nối API');  }}</code></pre><p>Thư viện <b>http</b> rất phổ biến để xử lý yêu cầu GET, POST, PUT, DELETE.</p><hr><h2>4. Lưu dữ liệu cục bộ với SharedPreferences</h2><pre><code>import 'package:shared_preferences/shared_preferences.dart';Future saveData() async {  final prefs = await SharedPreferences.getInstance();  await prefs.setString('username', 'nguyenxuanA');}Future readData() async {  final prefs = await SharedPreferences.getInstance();  print(prefs.getString('username'));}</code></pre><p><b>SharedPreferences</b> giúp lưu trữ dữ liệu nhỏ dưới dạng key–value, rất tiện cho token hoặc setting người dùng.</p><hr><h2>5. Quản lý trạng thái với Provider</h2><pre><code>import 'package:flutter/material.dart';import 'package:provider/provider.dart';class Counter with ChangeNotifier {  int _count = 0;  int get count =&gt; _count;  void increment() {    _count++;    notifyListeners();  }}void main() {  runApp(    ChangeNotifierProvider(      create: (_) =&gt; Counter(),      child: const MyApp(),    ),  );}class MyApp extends StatelessWidget {  const MyApp({super.key});  @override  Widget build(BuildContext context) {    final counter = Provider.of(context);    return MaterialApp(      home: Scaffold(        appBar: AppBar(title: const Text('Provider Demo')),        body: Center(child: Text('Count: ${counter.count}')),        floatingActionButton: FloatingActionButton(          onPressed: counter.increment,          child: const Icon(Icons.add),        ),      ),    );  }}</code></pre><p><b>Provider</b> giúp tách biệt logic và giao diện, dễ quản lý và mở rộng.</p><hr><h2>6. Một số gói phổ biến khác</h2><ul>  <li><b>url_launcher:</b> Mở liên kết web, gọi điện, gửi email.</li>  <li><b>firebase_core + firebase_auth:</b> Kết nối và xác thực Firebase.</li>  <li><b>flutter_local_notifications:</b> Hiển thị thông báo cục bộ.</li>  <li><b>image_picker:</b> Chọn ảnh từ camera hoặc thư viện.</li>  <li><b>path_provider:</b> Truy cập đường dẫn lưu trữ nội bộ.</li></ul><hr><h2>7. Tạo và xuất bản package</h2><ol>  <li>Tạo gói mới: <code>flutter create --template=package my_package</code></li>  <li>Viết code trong thư mục <code>lib/</code>.</li>  <li>Chạy lệnh kiểm tra: <code>flutter pub publish --dry-run</code></li>  <li>Đăng nhập tài khoản Google và xuất bản lên pub.dev.</li></ol><hr><h2>🧠 Bài tập thực hành</h2><ol>  <li>Tạo ứng dụng Flutter hiển thị dữ liệu JSON từ API công khai bằng http.</li>  <li>Lưu tên người dùng bằng SharedPreferences và hiển thị khi mở lại app.</li>  <li>Tạo ứng dụng đếm số có Provider quản lý trạng thái.</li>  <li>Thử dùng url_launcher mở liên kết đến trang Flutter.dev.</li></ol><hr><h2>✅ Tổng kết</h2><ul>  <li>Pub.dev là kho thư viện chính thức của Flutter/Dart.</li>  <li>Thêm thư viện vào pubspec.yaml giúp mở rộng tính năng app.</li>  <li>Các gói phổ biến: http, provider, shared_preferences, firebase_core.</li>  <li>Quản lý đúng phiên bản giúp ứng dụng ổn định và an toàn.</li></ul>",
  "updated_at": null
}