{
  "id": "lesson_bai-06-lap-trinh-mang-va-co-so-du-lieu",
  "title": "lesson_bai-06-lap-trinh-mang-va-co-so-du-lieu",
  "body": "<h1>🧩 BÀI 06 – LẬP TRÌNH MẠNG VÀ CƠ SỞ DỮ LIỆU</h1><h2>🎯 Mục tiêu học tập</h2><ul>  <li>Hiểu khái niệm lập trình mạng trong Flutter và mô hình client–server.</li>  <li>Biết cách gửi và nhận dữ liệu qua REST API bằng gói <b>http</b>.</li>  <li>Phân tích cấu trúc dữ liệu JSON và cách parse trong Dart.</li>  <li>Nắm vững cách lưu trữ cục bộ với SQLite và SharedPreferences.</li>  <li>Kết nối ứng dụng với Firebase Realtime Database hoặc Firestore.</li></ul><hr><h2>1. Giới thiệu lập trình mạng trong Flutter</h2><p>Flutter cho phép ứng dụng giao tiếp với máy chủ thông qua HTTP, WebSocket hoặc gRPC. Mô hình thường dùng là <b>Client–Server</b>.</p><ul>  <li><b>Client (Flutter App):</b> gửi request, nhận response từ server.</li>  <li><b>Server:</b> xử lý yêu cầu và trả kết quả (thường dạng JSON).</li></ul><hr><h2>2. Gọi REST API bằng gói HTTP</h2><pre><code>import 'package:http/http.dart' as http;import 'dart:convert';Future fetchUsers() async {  var url = Uri.parse('https://jsonplaceholder.typicode.com/users');  var response = await http.get(url);  if (response.statusCode == 200) {    var data = jsonDecode(response.body);    print('Số lượng user: ${data.length}');  } else {    print('Lỗi tải dữ liệu!');  }}</code></pre><p><b>Chú ý:</b> Khi dùng http, nên bao quanh bằng <code>try-catch</code> để tránh lỗi mạng.</p><hr><h2>3. Xử lý dữ liệu JSON</h2><p>Dữ liệu phản hồi API thường là JSON. Sử dụng thư viện <b>dart:convert</b> để parse.</p><pre><code>var jsonString = '{\"name\": \"An\", \"age\": 21}';var user = jsonDecode(jsonString);print(user['name']); // An</code></pre><p>Chuyển đối tượng sang JSON:</p><pre><code>var data = {'id': 1, 'title': 'Flutter'};print(jsonEncode(data)); // {\"id\":1,\"title\":\"Flutter\"}</code></pre><hr><h2>4. Lưu trữ dữ liệu cục bộ với SQLite</h2><pre><code>import 'package:sqflite/sqflite.dart';import 'package:path/path.dart';Future initDB() async {  final dbPath = await getDatabasesPath();  return openDatabase(    join(dbPath, 'students.db'),    onCreate: (db, version) {      return db.execute(        'CREATE TABLE students(id INTEGER PRIMARY KEY, name TEXT, score REAL)',      );    },    version: 1,  );}</code></pre><p>Thêm dữ liệu:</p><pre><code>await db.insert('students', {'name': 'Lan', 'score': 9.5});</code></pre><p>Truy vấn dữ liệu:</p><pre><code>final result = await db.query('students');print(result);</code></pre><hr><h2>5. Lưu trữ dữ liệu trên Firebase</h2><pre><code>import 'package:firebase_core/firebase_core.dart';import 'package:cloud_firestore/cloud_firestore.dart';Future addData() async {  await Firebase.initializeApp();  await FirebaseFirestore.instance.collection('users').add({    'name': 'Minh',    'email': 'minh@gmail.com'  });}</code></pre><p>Lắng nghe realtime:</p><pre><code>FirebaseFirestore.instance.collection('users').snapshots().listen((snapshot) {  for (var doc in snapshot.docs) {    print(doc['name']);  }});</code></pre><hr><h2>6. So sánh SQLite và Firebase</h2><table border=\"1\" cellpadding=\"6\"><thead><tr><th>Tiêu chí</th><th>SQLite</th><th>Firebase</th></tr></thead><tbody><tr><td>Loại lưu trữ</td><td>Cục bộ (offline)</td><td>Trực tuyến (cloud)</td></tr><tr><td>Đồng bộ dữ liệu</td><td>Không tự động</td><td>Có realtime</td></tr><tr><td>Phù hợp</td><td>Ứng dụng offline</td><td>Ứng dụng online đa người dùng</td></tr><tr><td>Tốc độ</td><td>Rất nhanh</td><td>Phụ thuộc mạng</td></tr></tbody></table><hr><h2>7. Xử lý lỗi mạng</h2><pre><code>try {  var res = await http.get(Uri.parse('https://example.com'));  if (res.statusCode == 200) {    print('Kết nối thành công!');  }} catch (e) {  print('Không có kết nối mạng: $e');}</code></pre><hr><h2>🧠 Bài tập thực hành</h2><ol>  <li>Tạo ứng dụng hiển thị danh sách người dùng từ API JSONPlaceholder.</li>  <li>Lưu danh sách sinh viên vào SQLite và hiển thị lên ListView.</li>  <li>Tích hợp Firebase để lưu và đọc dữ liệu người dùng realtime.</li>  <li>Thêm xử lý lỗi mạng và hiển thị thông báo lỗi bằng SnackBar.</li></ol><hr><h2>✅ Tổng kết</h2><ul>  <li>Flutter hỗ trợ mạng qua gói http.</li>  <li>JSON là định dạng trao đổi dữ liệu chính giữa client và server.</li>  <li>SQLite lưu offline, Firebase lưu cloud realtime.</li>  <li>Kết hợp đúng giúp ứng dụng hoạt động tốt cả online và offline.</li></ul>",
  "updated_at": null
}