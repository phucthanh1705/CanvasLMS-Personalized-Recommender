{
  "id": "lesson_bai-2-quan-lu-cac-luong-nhap-xuat",
  "title": "lesson_bai-2-quan-lu-cac-luong-nhap-xuat",
  "body": "<h1>💾 BÀI 2 – QUẢN LÝ CÁC LUỒNG NHẬP XUẤT</h1><h2>2.1 GIỚI THIỆU</h2><p>Trong lập trình mạng, dữ liệu được trao đổi giữa các tiến trình thông qua <b>luồng (stream)</b>. Luồng biểu diễn dòng chảy của dữ liệu từ nguồn đến đích, ví dụ: từ bàn phím đến bộ nhớ, từ file đến socket, hoặc giữa các ứng dụng mạng.</p><p>Java cung cấp một hệ thống I/O mạnh mẽ với các lớp trong gói <code>java.io</code> và <code>java.nio</code>.</p><ul><li><b>Luồng vào (Input Stream):</b> đọc dữ liệu từ nguồn.</li><li><b>Luồng ra (Output Stream):</b> ghi dữ liệu đến đích.</li></ul><hr><h2>2.2 CÁC LUỒNG BYTE</h2><p>Các luồng byte làm việc trực tiếp với dữ liệu nhị phân, thích hợp cho đọc/ghi file, socket, và dữ liệu mạng.</p><h3>2.2.1 Các lớp cơ bản</h3><ul><li><code>InputStream</code>: lớp cha cho tất cả luồng nhập byte.</li><li><code>OutputStream</code>: lớp cha cho tất cả luồng xuất byte.</li></ul><p>Ví dụ đọc file nhị phân:</p><pre><code>FileInputStream fin = new FileInputStream(\"data.bin\");\nint b;\nwhile((b = fin.read()) != -1) {\n    System.out.print((char)b);\n}\nfin.close();</code></pre><h3>2.2.2 Các luồng đọc/ghi hiện thực</h3><ul><li><code>FileInputStream</code> / <code>FileOutputStream</code></li><li><code>BufferedInputStream</code> / <code>BufferedOutputStream</code> – dùng bộ đệm để tăng hiệu suất.</li><li><code>DataInputStream</code> / <code>DataOutputStream</code> – đọc/ghi kiểu dữ liệu nguyên thủy.</li></ul><p>Ví dụ ghi dữ liệu kiểu:</p><pre><code>DataOutputStream out = new DataOutputStream(new FileOutputStream(\"user.dat\"));\nout.writeInt(101);\nout.writeUTF(\"Network Programming\");\nout.close();</code></pre><hr><h2>2.3 CÁC LUỒNG KÝ TỰ</h2><p>Các luồng ký tự làm việc với dữ liệu text (Unicode). Java tách riêng để xử lý các vấn đề mã hóa.</p><ul><li><code>Reader</code>: luồng nhập ký tự.</li><li><code>Writer</code>: luồng xuất ký tự.</li></ul><p>Ví dụ đọc text:</p><pre><code>FileReader fr = new FileReader(\"input.txt\");\nint c;\nwhile((c = fr.read()) != -1) {\n    System.out.print((char)c);\n}\nfr.close();</code></pre><p>Luồng ký tự thường dùng cho file log, cấu hình hoặc giao thức dạng text (HTTP, SMTP).</p><hr><h2>2.4 CÁC LUỒNG LỌC DỮ LIỆU</h2><p>Java hỗ trợ <b>luồng lọc (Filter Stream)</b> để mở rộng chức năng luồng cơ bản mà không thay đổi cấu trúc chính. Một FilterStream bao bọc luồng khác.</p><ul><li><code>BufferedInputStream</code>: thêm bộ đệm.</li><li><code>DataInputStream</code>: đọc dữ liệu theo kiểu.</li><li><code>PushbackInputStream</code>: cho phép đẩy ngược byte vào luồng.</li></ul><pre><code>BufferedInputStream bis = new BufferedInputStream(new FileInputStream(\"config.ini\"));\nint data = bis.read();</code></pre><p>→ Lớp này giúp giảm truy cập I/O trực tiếp, tăng tốc độ xử lý.</p><hr><h2>2.5 CÁC LUỒNG ĐỆM DỮ LIỆU</h2><p>Luồng đệm (Buffered Stream) giúp giảm số lần truy cập thiết bị thật. Khi đọc/ghi file lớn hoặc socket, đây là lớp cực kỳ quan trọng.</p><ul><li><code>BufferedReader</code>, <code>BufferedWriter</code> (text)</li><li><code>BufferedInputStream</code>, <code>BufferedOutputStream</code> (byte)</li></ul><p>Ví dụ:</p><pre><code>BufferedReader br = new BufferedReader(new FileReader(\"input.txt\"));\nString line;\nwhile((line = br.readLine()) != null) {\n    System.out.println(line);\n}\nbr.close();</code></pre><hr><h2>2.6 CÁC LỚP NHẬP/XUẤT ĐỊNH KIỂU DỮ LIỆU</h2><p>Để làm việc với các kiểu dữ liệu cụ thể (int, float, UTF, boolean...), Java cung cấp:</p><ul><li><code>DataInputStream</code> và <code>DataOutputStream</code></li><li><code>ObjectInputStream</code> và <code>ObjectOutputStream</code> để tuần tự hóa đối tượng.</li></ul><p>Ví dụ tuần tự hóa:</p><pre><code>ObjectOutputStream oos = new ObjectOutputStream(new FileOutputStream(\"user.obj\"));\noos.writeObject(new User(\"Alice\", 22));\noos.close();</code></pre><p>→ Đây là nền tảng để gửi đối tượng qua mạng (sẽ học trong bài 6).</p><hr><h2>✅ TỔNG KẾT</h2><ul><li>Java I/O phân chia thành luồng byte và luồng ký tự.</li><li>Có thể kết hợp các luồng bằng kỹ thuật wrapping (gói trong gói).</li><li>Luồng đệm giúp tăng hiệu suất đáng kể.</li><li>Các luồng định kiểu hỗ trợ xử lý dữ liệu mạnh mẽ, linh hoạt.</li></ul>",
  "updated_at": null
}