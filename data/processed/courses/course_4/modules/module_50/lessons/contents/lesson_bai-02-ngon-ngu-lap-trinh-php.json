{
  "id": "lesson_bai-02-ngon-ngu-lap-trinh-php",
  "title": "lesson_bai-02-ngon-ngu-lap-trinh-php",
  "body": "<h1>🧩 BÀI 02 – NGÔN NGỮ LẬP TRÌNH PHP</h1><h2>🎯 Mục tiêu học tập</h2><ul> <li>Hiểu cú pháp cơ bản, biến, kiểu dữ liệu, cấu trúc điều khiển của PHP.</li> <li>Biết cách sử dụng hàm, mảng, và chuỗi trong PHP.</li> <li>Áp dụng lập trình hướng đối tượng (OOP) trong PHP 8+.</li> <li>Thực hành xử lý biểu mẫu (Form Handling) và gửi dữ liệu giữa client-server.</li> <li>Kết nối PHP với cơ sở dữ liệu MySQL để thực hiện CRUD.</li></ul><hr><h2>1. Giới thiệu PHP</h2><p><b>PHP (Hypertext Preprocessor)</b> là ngôn ngữ lập trình phía máy chủ (server-side) được nhúng trực tiếp trong mã HTML, thường dùng để phát triển ứng dụng web động.</p><p><b>Đặc điểm chính:</b></p><ul> <li>Mã nguồn mở, chạy đa nền tảng (Windows, Linux, macOS).</li> <li>Dễ học, tích hợp chặt chẽ với HTML và MySQL.</li> <li>Hỗ trợ OOP, xử lý HTTP, JSON, RESTful API, session, cookie.</li></ul><pre><code>&lt;?php echo \"Xin chào thế giới!\";?&gt;</code></pre><p><b>Output:</b> Xin chào thế giới!</p><hr><h2>2. Biến và kiểu dữ liệu</h2><p>Trong PHP, biến bắt đầu bằng ký tự <code>$</code>.</p><pre><code>$ten = \"An\";$tuoi = 20;$diemTB = 8.5;$coBan = true;</code></pre><p><b>Kiểu dữ liệu:</b> string, integer, float, boolean, array, object, null.</p><hr><h2>3. Cấu trúc điều khiển</h2><p>PHP hỗ trợ các cấu trúc như if, switch, for, foreach, while.</p><pre><code>for ($i = 1; $i &lt;= 5; $i++) { echo \"Dòng thứ $i&lt;br&gt;\";}</code></pre><hr><h2>4. Mảng và hàm</h2><pre><code>$arr = array(\"PHP\", \"Python\", \"Java\");foreach ($arr as $lang) { echo $lang . \" &lt;br&gt;\";}function tong($a, $b) { return $a + $b;}echo tong(5, 7); // Output: 12</code></pre><p><b>PHP hỗ trợ:</b> mảng chỉ số (indexed), kết hợp (associative), và đa chiều (multidimensional).</p><hr><h2>5. Lập trình hướng đối tượng (OOP trong PHP)</h2><pre><code>class SinhVien { public $ten; public $lop; function __construct($ten, $lop) { $this-&gt;ten = $ten; $this-&gt;lop = $lop; } function hienThi() { echo \"SV: $this-&gt;ten - Lớp: $this-&gt;lop\"; }}$sv = new SinhVien(\"Minh\", \"CNTT1\");$sv-&gt;hienThi();</code></pre><p><b>Kết quả:</b> SV: Minh - Lớp: CNTT1</p><p>PHP hỗ trợ tính kế thừa, đa hình, trừu tượng và interface tương tự các ngôn ngữ OOP khác.</p><hr><h2>6. Xử lý form (Form Handling)</h2><pre><code>&lt;form method=\"POST\" action=\"xuly.php\"&gt; Tên: &lt;input type=\"text\" name=\"ten\"&gt; &lt;input type=\"submit\" value=\"Gửi\"&gt;&lt;/form&gt;</code></pre><pre><code>// xuly.php$ten = $_POST['ten'];echo \"Xin chào, $ten!\";</code></pre><p>PHP nhận dữ liệu qua <code>$_GET</code>, <code>$_POST</code>, hoặc <code>$_REQUEST</code>.</p><hr><h2>7. Kết nối MySQL (MySQLi &amp; PDO)</h2><pre><code>$conn = new mysqli(\"localhost\", \"root\", \"\", \"qlsv\");if ($conn-&gt;connect_error) die(\"Lỗi kết nối!\");$sql = \"SELECT * FROM sinhvien\";$result = $conn-&gt;query($sql);while($row = $result-&gt;fetch_assoc()) { echo $row['HoTen'] . \"&lt;br&gt;\";}</code></pre><p>PHP hỗ trợ hai thư viện chính: <b>MySQLi (thủ tục / hướng đối tượng)</b> và <b>PDO (đa hệ quản trị CSDL)</b>.</p><hr><h2>8. Bảo mật cơ bản trong PHP</h2><ul> <li>Luôn kiểm tra đầu vào (Input Validation).</li> <li>Dùng <code>htmlspecialchars()</code> tránh XSS.</li> <li>Dùng Prepared Statement để chống SQL Injection.</li></ul><hr><h2>🧠 Bài tập thực hành</h2><ol> <li>Viết chương trình tính tổng hai số nhập từ form.</li> <li>Viết lớp <code>SanPham</code> có thuộc tính tên và giá, kèm hàm hiển thị.</li> <li>Tạo form đăng nhập và xử lý xác thực người dùng.</li> <li>Kết nối MySQL, hiển thị danh sách sinh viên trong bảng.</li></ol><hr><h2>✅ Tổng kết</h2><ul> <li>PHP là nền tảng server-side mạnh mẽ, dễ tích hợp với HTML và MySQL.</li> <li>Hỗ trợ OOP, xử lý form, kết nối CSDL, và bảo mật cơ bản.</li> <li>Hiểu và thực hành PHP là bước nền cho các CMS mã nguồn mở (Joomla, WordPress, Moodle).</li></ul>",
  "updated_at": null
}