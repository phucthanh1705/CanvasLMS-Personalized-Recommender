{
  "title": "BÀI 2 – CON TRỎ (POINTERS)",
  "created_at": "2025-10-19T09:19:09Z",
  "url": "bai-2-con-tro-pointers",
  "editing_roles": "teachers",
  "page_id": 203,
  "last_edited_by": {
    "id": 1,
    "anonymous_id": "1",
    "display_name": "admin@gmail.com",
    "avatar_image_url": "http://localhost:3000/images/messages/avatar-50.png",
    "html_url": "http://localhost:3000/courses/4/users/1",
    "pronouns": null
  },
  "published": true,
  "hide_from_students": false,
  "front_page": false,
  "html_url": "http://localhost:3000/courses/4/pages/bai-2-con-tro-pointers",
  "todo_date": null,
  "publish_at": null,
  "updated_at": "2025-10-19T09:19:25Z",
  "locked_for_user": false,
  "body": "<h1>🧩 BÀI 2 – CON TRỎ (POINTERS)</h1><h2>🎯 Mục tiêu học tập</h2><ul><li>Hiểu khái niệm con trỏ và vai trò của nó trong truy xuất bộ nhớ.</li><li>Biết cách khai báo, khởi tạo và sử dụng con trỏ trong ngôn ngữ C.</li><li>Vận dụng con trỏ với mảng, chuỗi, cấu trúc và hàm.</li><li>Hiểu nguyên lý cấp phát và thu hồi bộ nhớ động, tránh lỗi con trỏ.</li></ul><hr><h2>1. Khái niệm con trỏ</h2><p><b>Con trỏ (Pointer)</b> là biến đặc biệt dùng để lưu trữ <b>địa chỉ ô nhớ</b> của một biến khác. Mỗi biến đều được lưu tại một vùng nhớ xác định, và con trỏ giúp ta thao tác trực tiếp với vùng nhớ đó.</p><pre><code>int x = 10; int *p = &amp;x; printf(\"%d\", *p); // In ra giá trị 10</code></pre><ul><li>Toán tử <code>&amp;</code> lấy địa chỉ của biến.</li><li>Toán tử <code>*</code> (dereference) truy cập giá trị tại địa chỉ mà con trỏ trỏ tới.</li></ul><hr><h2>2. Khai báo và khởi tạo con trỏ</h2><pre><code>int *p; float *f; char *s;</code></pre><p>Con trỏ phải được gán địa chỉ hợp lệ trước khi sử dụng:</p><pre><code>int a = 5; int *p = &amp;a; printf(\"%d\", *p); // In ra 5</code></pre><p>Nếu con trỏ chưa được gán địa chỉ, nó có thể trỏ đến vùng nhớ rác. Để tránh lỗi, luôn khởi tạo bằng <code>NULL</code>:</p><pre><code>int *ptr = NULL;</code></pre><hr><h2>3. Các phép toán trên con trỏ</h2><h3>3.1. Phép gán</h3><pre><code>int *p1, *p2; int x = 10; p1 = &amp;x; p2 = p1;</code></pre><h3>3.2. Phép tăng/giảm con trỏ</h3><p>Khi tăng, con trỏ di chuyển sang ô nhớ tiếp theo theo kích thước kiểu dữ liệu.</p><pre><code>int a[5] = {10,20,30,40,50}; int *p = a; p++; printf(\"%d\", *p); // In ra 20</code></pre><h3>3.3. Phép so sánh</h3><pre><code>if(p1 == p2) printf(\"Hai con trỏ cùng địa chỉ\");</code></pre><h3>3.4. Truy cập dữ liệu qua con trỏ</h3><pre><code>*(p+2) = 100; // Tương đương a[2] = 100;</code></pre><hr><h2>4. Con trỏ và mảng</h2><p>Tên mảng là một con trỏ hằng trỏ đến phần tử đầu tiên của mảng.</p><pre><code>int a[3] = {1,2,3}; int *p = a; printf(\"%d\", *(p+1)); // 2</code></pre><p>Tuy nhiên, không thể gán ngược lại <code>a = p;</code> vì tên mảng là hằng địa chỉ.</p><hr><h2>5.Con trỏ và chuỗi ký tự</h2><p>Chuỗi có thể được xử lý bằng con trỏ, giúp tiết kiệm bộ nhớ và dễ thao tác hơn.</p><pre><code>char *s = \"Hello\"; while(*s != '\\0'){ printf(\"%c\", *s); s++; }</code></pre><p>Thư viện <code>&lt;string.h&gt;</code> cho phép thao tác chuỗi qua con trỏ như <code>strlen()</code>, <code>strcpy()</code>, <code>strcmp()</code>.</p><hr><h2>6. Con trỏ và cấu trúc (struct)</h2><pre><code>typedef struct { char name[20]; int age; } Student; Student s1 = {\"An\", 20}; Student *p = &amp;s1; printf(\"%s - %d\", p-&gt;name, p-&gt;age);</code></pre><p>Toán tử <b>-&gt;</b> được dùng để truy cập thành phần của struct thông qua con trỏ.</p><hr><h2>7. Con trỏ hàm</h2><p>Cho phép lưu địa chỉ của hàm và gọi gián tiếp:</p><pre><code>int add(int a, int b){ return a+b; } int (*fp)(int, int) = add; printf(\"%d\", fp(3,4));</code></pre><p>Ứng dụng: <i>callback, xử lý đa hình trong C, lập trình hướng sự kiện</i>.</p><hr><h2>8. Cấp phát và thu hồi bộ nhớ động</h2><p>Dùng thư viện <code>&lt;stdlib.h&gt;</code>:</p><pre><code>int *a = (int*) malloc(n * sizeof(int)); if(a == NULL) exit(1); for(int i=0;i</code></pre><p><code><b>Lưu ý:</b> Sau khi <code>free()</code>, cần gán con trỏ về NULL để tránh con trỏ treo (dangling pointer).</code></p><hr><h2><code>9. Con trỏ tới con trỏ</code></h2><pre><code><code>int x = 10; int *p = &amp;x; int **q = &amp;p; printf(\"%d\", **q); // 10</code></code></pre><p><code>Con trỏ tới con trỏ lưu địa chỉ của một con trỏ khác.</code></p><hr><h2><code>10. Một số lỗi thường gặp</code></h2><ul><li><code>Truy cập con trỏ NULL (Segmentation fault).</code></li><li><code>Sử dụng con trỏ sau khi free().</code></li><li><code>Cấp phát mà không thu hồi bộ nhớ (Memory Leak).</code></li><li><code>Gán sai kiểu con trỏ.</code></li></ul><hr><h2><code>✅ Kết luận</code></h2><p><code>Con trỏ là công cụ mạnh mẽ trong C, giúp lập trình viên quản lý dữ liệu, truyền tham chiếu và tạo cấu trúc dữ liệu động. Tuy nhiên, nếu sử dụng sai, nó có thể gây lỗi nghiêm trọng về bộ nhớ và logic chương trình.</code></p>"
}