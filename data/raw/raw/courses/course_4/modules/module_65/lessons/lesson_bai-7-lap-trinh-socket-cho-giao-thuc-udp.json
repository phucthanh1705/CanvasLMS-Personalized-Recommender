{
  "title": "BÀI 7 – LẬP TRÌNH SOCKET CHO GIAO THỨC UDP",
  "created_at": "2025-10-25T14:05:48Z",
  "url": "bai-7-lap-trinh-socket-cho-giao-thuc-udp",
  "editing_roles": "teachers",
  "page_id": 294,
  "last_edited_by": {
    "id": 1,
    "anonymous_id": "1",
    "display_name": "admin@gmail.com",
    "avatar_image_url": "http://localhost:3000/images/messages/avatar-50.png",
    "html_url": "http://localhost:3000/courses/4/users/1",
    "pronouns": null
  },
  "published": true,
  "hide_from_students": false,
  "front_page": false,
  "html_url": "http://localhost:3000/courses/4/pages/bai-7-lap-trinh-socket-cho-giao-thuc-udp",
  "todo_date": null,
  "publish_at": null,
  "updated_at": "2025-10-25T14:06:03Z",
  "locked_for_user": false,
  "body": "<h1>📡 BÀI 7 – LẬP TRÌNH SOCKET CHO GIAO THỨC UDP</h1><h2>7.1 GIỚI THIỆU GIAO THỨC UDP</h2><p><b>UDP (User Datagram Protocol)</b> là giao thức tầng Transport trong mô hình TCP/IP, tương tự TCP nhưng <b>không đảm bảo độ tin cậy</b>. Dữ liệu được gửi theo từng gói (datagram) mà không cần thiết lập kết nối trước.</p><ul><li>Không có cơ chế xác nhận (ACK).</li><li>Không đảm bảo gói đến đúng thứ tự.</li><li>Tốc độ truyền nhanh, phù hợp ứng dụng thời gian thực.</li></ul><p>⚙️ <b>Ứng dụng thực tế:</b> Truyền video, âm thanh, game online, DNS, VoIP.</p><hr><h2>7.2 MÔ HÌNH GỬI – NHẬN DỮ LIỆU UDP</h2><p>UDP sử dụng hai lớp chính trong Java:</p><ul><li><b>DatagramSocket</b>: tạo cổng giao tiếp để gửi/nhận dữ liệu.</li><li><b>DatagramPacket</b>: đại diện cho một gói dữ liệu UDP.</li></ul><h3>📦 Cấu trúc gói UDP:</h3><pre><code>| Header (8 bytes) | Data |\nHeader = Source Port + Destination Port + Length + Checksum</code></pre><hr><h2>7.3 LỚP DATAGRAMSOCKET</h2><p><code>DatagramSocket</code> được dùng để gửi hoặc nhận gói UDP.</p><pre><code>DatagramSocket socket = new DatagramSocket(); // client\nDatagramSocket server = new DatagramSocket(9999); // server</code></pre><p>Các phương thức quan trọng:</p><ul><li><code>send(DatagramPacket)</code>: gửi gói dữ liệu.</li><li><code>receive(DatagramPacket)</code>: nhận gói dữ liệu.</li><li><code>close()</code>: đóng socket.</li></ul><hr><h2>7.4 LỚP DATAGRAMPACKET</h2><p><code>DatagramPacket</code> chứa dữ liệu và địa chỉ đích.</p><pre><code>byte[] data = \"Hello UDP\".getBytes();\nInetAddress ip = InetAddress.getByName(\"localhost\");\nDatagramPacket packet = new DatagramPacket(data, data.length, ip, 9999);\nsocket.send(packet);</code></pre><p>Trên phía nhận:</p><pre><code>byte[] buffer = new byte[1024];\nDatagramPacket packet = new DatagramPacket(buffer, buffer.length);\nsocket.receive(packet);\nString msg = new String(packet.getData(), 0, packet.getLength());\nSystem.out.println(\"Nhận: \" + msg);</code></pre><hr><h2>7.5 VÍ DỤ CHƯƠNG TRÌNH UDP</h2><h3>UDP Server</h3><pre><code>import java.net.*;\npublic class UDPServer {\n    public static void main(String[] args) throws Exception {\n        DatagramSocket server = new DatagramSocket(9999);\n        byte[] buf = new byte[1024];\n        System.out.println(\"Server đang chờ dữ liệu UDP...\");\n        while(true){\n            DatagramPacket packet = new DatagramPacket(buf, buf.length);\n            server.receive(packet);\n            String msg = new String(packet.getData(), 0, packet.getLength());\n            System.out.println(\"Client: \" + msg);\n            byte[] reply = (\"Server nhận: \" + msg).getBytes();\n            DatagramPacket response = new DatagramPacket(reply, reply.length, packet.getAddress(), packet.getPort());\n            server.send(response);\n        }\n    }\n}</code></pre><h3>UDP Client</h3><pre><code>import java.net.*;\npublic class UDPClient {\n    public static void main(String[] args) throws Exception {\n        DatagramSocket socket = new DatagramSocket();\n        InetAddress ip = InetAddress.getByName(\"localhost\");\n        byte[] data = \"Xin chào UDP Server\".getBytes();\n        DatagramPacket packet = new DatagramPacket(data, data.length, ip, 9999);\n        socket.send(packet);\n\n        byte[] buf = new byte[1024];\n        DatagramPacket reply = new DatagramPacket(buf, buf.length);\n        socket.receive(reply);\n        System.out.println(\"Server trả lời: \" + new String(reply.getData(), 0, reply.getLength()));\n        socket.close();\n    }\n}</code></pre><hr><h2>7.6 UDP BROADCAST</h2><p>UDP cho phép gửi dữ liệu đến nhiều host trong cùng mạng qua <b>broadcast address</b> (ví dụ: 192.168.1.255).</p><pre><code>socket.setBroadcast(true);\nInetAddress broadcast = InetAddress.getByName(\"192.168.1.255\");\nDatagramPacket packet = new DatagramPacket(data, data.length, broadcast, 9999);\nsocket.send(packet);</code></pre><p>⚠️ Cần cấu hình router hoặc firewall cho phép broadcast.</p><hr><h2>7.7 ƯU NHƯỢC ĐIỂM CỦA UDP</h2><p><b>Ưu điểm:</b></p><ul><li>Tốc độ cao, độ trễ thấp.</li><li>Không yêu cầu handshake như TCP.</li><li>Thích hợp truyền dữ liệu liên tục.</li></ul><p><b>Nhược điểm:</b></p><ul><li>Không đảm bảo dữ liệu đến đích.</li><li>Không có cơ chế xác nhận hoặc kiểm soát luồng.</li><li>Không sắp xếp lại gói tin bị đảo lộn.</li></ul><hr><h2>✅ TỔNG KẾT</h2><ul><li>UDP là giao thức không kết nối, truyền dữ liệu dạng gói (datagram).</li><li>Java hỗ trợ UDP thông qua lớp <code>DatagramSocket</code> và <code>DatagramPacket</code>.</li><li>Phù hợp ứng dụng cần tốc độ hơn độ tin cậy.</li><li>Cần xử lý lỗi và mất gói ở tầng ứng dụng nếu cần bảo đảm.</li></ul>"
}